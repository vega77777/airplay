# 飛出台灣：全球限時特惠機票情報站
## 完整強化版規劃方案 v2.0

---

## 一、核心定位與差異化策略

### 1.1 產品定位
**「快、準、直達」三位一體**：不與 Skyscanner、Google Flights 拼常態比價，專攻各大航空公司的**限時促銷（Flash Sales）**。

| 對比維度 | 傳統比價網 | 本站定位 |
|---|---|---|
| 核心功能 | 即時比價、路線搜尋 | 促銷情報聚合、雷達推播 |
| 資料來源 | 航空公司 GDS/API | 官網促銷頁 + 社群媒體 + 電子報 |
| 商業模式 | OTA 佣金抽成 | 聯盟行銷 + 廣告 + 訂閱 |
| 用戶行為 | 主動查詢 | 被動接收推播、主動篩選 |
| 訂票方式 | 站內完成 | 直達官方訂票頁（Deep Link）|

### 1.2 目標用戶輪廓
- **主力族群**：25–40 歲、有固定工作的旅遊愛好者，擅長自助旅遊
- **次要族群**：學生背包客、有彈性假期的自由工作者
- **用戶痛點**：
  1. 錯過限時優惠（看到時已售完）
  2. 分散追蹤多個航空公司官方帳號太累
  3. 不清楚含稅後實際要付多少

---

## 二、完整網站架構（強化版）

### 2.1 頁面地圖（Sitemap）

```
飛出台灣
├── 首頁（特惠看板）
│   ├── Hero：今日最殺促銷輪播
│   ├── 即時動態牆（瀑布流卡片）
│   ├── 熱門目的地快篩
│   └── 倒數計時促銷卡
├── 目的地探索
│   ├── 東北亞（日本、韓國）
│   ├── 東南亞（泰國、菲律賓、越南⋯）
│   ├── 歐洲
│   ├── 美洲
│   └── 全球地圖視覺化
├── 航空公司情報中心
│   ├── 台灣本籍航空（長榮、華航、星宇、虎航）
│   ├── 亞洲廉航（樂桃、酷航、亞航、捷星）
│   ├── 每家公司的促銷歷史規律分析
│   └── 現有折扣碼 / 信用卡優惠整理
├── 促銷詳情頁（Detail Page）
│   ├── 促銷標題 + 航線資訊
│   ├── 含稅預估價試算器
│   ├── 購票時間軸（開賣 / 截止 / 旅遊日期）
│   └── 官方訂票直達按鈕（Deep Link）
├── 雷達訂閱中心
│   ├── 目的地偏好設定
│   ├── 預算上限設定
│   ├── 推播管道選擇（LINE / Telegram / Email）
│   └── 訂閱紀錄管理
├── 機票知識庫（SEO 內容）
│   ├── 各航空廉航促銷規律整理
│   ├── 訂票教學（如何搶到最低價）
│   └── 行李、轉機注意事項
└── 關於我們 / 聯絡
```

### 2.2 首頁功能細節強化

**Hero Section（今日最殺）**
- 輪播限時 ≤ 24 小時的最緊急促銷
- 顯示欄位：航線 ✈ 目的地城市 | 票價 | 倒數計時 | 「立即搶購」CTA 按鈕
- 動態背景：對應目的地的城市景點圖（自動從 Unsplash API 拉取）

**即時動態牆（強化功能）**

```
篩選條件（多選）：
- 目的地洲別：[東北亞] [東南亞] [歐洲] [美洲] [大洋洲]
- 航空類型：[傳統航空] [廉價航空]
- 航空公司：[長榮] [華航] [星宇] [虎航] [樂桃] [酷航]...
- 票價範圍：[< $3,000] [$3,000-$6,000] [$6,000-$12,000] [> $12,000]
- 旅遊月份：[月份選擇器]
- 排序方式：[最新上架] [折扣最高] [點擊最熱] [最快截止]
```

**促銷卡片資訊架構**

```
┌──────────────────────────────────────┐
│ [航空公司 Logo] 虎航          [廉航]  │
│ 台北 TPE → 大阪 KIX                  │
│                                      │
│  NT$ 1,499 起  ~~NT$ 4,200~~         │
│  含稅約 NT$ 2,800（達人試算）         │
│                                      │
│  🗓 旅遊期間：7/1 – 9/30             │
│  ⏰ 距離截止：14:23:05               │
│  🔥 今日已有 892 人點擊              │
│                                      │
│  [🔔 加入雷達]  [→ 前往訂票（官網）] │
└──────────────────────────────────────┘
```

---

## 三、資料庫架構設計

### 3.1 核心資料表（Supabase / PostgreSQL）

```sql
-- 航空公司主表
CREATE TABLE airlines (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  code VARCHAR(3) UNIQUE NOT NULL,       -- IATA 代碼 (e.g. 'BR', 'CI')
  name_zh VARCHAR(50) NOT NULL,
  name_en VARCHAR(100),
  logo_url TEXT,
  official_url TEXT,
  promo_page_url TEXT,                   -- 促銷頁 URL（爬蟲目標）
  is_lcc BOOLEAN DEFAULT false,          -- 是否廉航
  is_active BOOLEAN DEFAULT true,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 機場主表
CREATE TABLE airports (
  iata_code CHAR(3) PRIMARY KEY,
  city_zh VARCHAR(50),
  city_en VARCHAR(100),
  country_code CHAR(2),
  country_zh VARCHAR(50),
  timezone VARCHAR(50),
  latitude DECIMAL(9,6),
  longitude DECIMAL(9,6)
);

-- 促銷主表（核心）
CREATE TABLE promotions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  airline_id UUID REFERENCES airlines(id),
  title TEXT NOT NULL,
  description TEXT,

  -- 航線資訊
  origin_airport CHAR(3) REFERENCES airports(iata_code),
  destination_airport CHAR(3) REFERENCES airports(iata_code),
  is_round_trip BOOLEAN DEFAULT true,

  -- 票價資訊
  price_base DECIMAL(10,2),              -- 促銷未稅價
  price_tax_estimated DECIMAL(10,2),     -- 達人試算含稅價
  currency CHAR(3) DEFAULT 'TWD',
  original_price DECIMAL(10,2),          -- 原價（用於計算折扣率）
  discount_percent SMALLINT,             -- 折扣百分比

  -- 時間資訊
  sale_start_at TIMESTAMPTZ,             -- 促銷開賣時間
  sale_end_at TIMESTAMPTZ,               -- 促銷截止時間
  travel_start_date DATE,                -- 適用旅遊起始日
  travel_end_date DATE,                  -- 適用旅遊截止日
  blackout_dates JSONB,                  -- 封鎖日期（不適用日期）

  -- 來源與連結
  source_url TEXT NOT NULL,              -- 爬蟲來源 URL
  booking_url TEXT,                      -- 訂票直達連結（Deep Link）
  promo_code VARCHAR(50),                -- 折扣碼（若有）

  -- 狀態與分類
  status VARCHAR(20) DEFAULT 'active',   -- active / expired / coming_soon
  region VARCHAR(20),                    -- northeast_asia / southeast_asia / europe / americas
  tags JSONB DEFAULT '[]',               -- 標籤如 ["限時24小時", "含稅低"]

  -- 互動數據
  click_count INTEGER DEFAULT 0,
  bookmark_count INTEGER DEFAULT 0,

  -- 系統欄位
  scraped_at TIMESTAMPTZ DEFAULT NOW(),
  verified_at TIMESTAMPTZ,               -- 人工或自動驗證時間
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- 爬蟲日誌
CREATE TABLE crawler_logs (
  id BIGSERIAL PRIMARY KEY,
  airline_id UUID REFERENCES airlines(id),
  source_url TEXT,
  status VARCHAR(20),                    -- success / failed / no_change
  new_promotions_count INTEGER DEFAULT 0,
  error_message TEXT,
  duration_ms INTEGER,
  executed_at TIMESTAMPTZ DEFAULT NOW()
);

-- 用戶雷達訂閱
CREATE TABLE radar_subscriptions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_identifier TEXT NOT NULL,         -- LINE user ID / Email / Telegram chat ID
  channel VARCHAR(20) NOT NULL,          -- line / telegram / email
  destination_regions JSONB DEFAULT '["all"]',
  airline_ids JSONB DEFAULT '["all"]',
  max_price INTEGER,                     -- 最高接受票價（TWD）
  is_active BOOLEAN DEFAULT true,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 促銷點擊追蹤
CREATE TABLE promotion_clicks (
  id BIGSERIAL PRIMARY KEY,
  promotion_id UUID REFERENCES promotions(id),
  session_id TEXT,
  clicked_at TIMESTAMPTZ DEFAULT NOW()
);

-- 索引優化
CREATE INDEX idx_promotions_status_sale_end ON promotions(status, sale_end_at);
CREATE INDEX idx_promotions_origin ON promotions(origin_airport);
CREATE INDEX idx_promotions_region ON promotions(region);
CREATE INDEX idx_promotions_scraped_at ON promotions(scraped_at DESC);
```

### 3.2 Supabase Realtime 設定

啟用 `promotions` 資料表的 Realtime，前端訂閱：
```javascript
// 前端訂閱新促銷即時推送
const channel = supabase
  .channel('new-promotions')
  .on('postgres_changes', {
    event: 'INSERT',
    schema: 'public',
    table: 'promotions',
    filter: 'status=eq.active'
  }, (payload) => {
    // 在畫面頂部彈出 Toast 通知
    showNewDealToast(payload.new)
  })
  .subscribe()
```

---

## 四、爬蟲架構（強化詳細版）

### 4.1 爬蟲目標清單（台灣起飛航空公司）

| 航空公司 | 促銷頁 URL | 爬蟲難度 | 建議工具 |
|---|---|---|---|
| 台灣虎航 | tigerairtw.com/promo | ⭐⭐ 中 | Playwright |
| 長榮航空 | evaair.com/zh-tw/promotions | ⭐⭐ 中 | Requests |
| 中華航空 | china-airlines.com/tw | ⭐⭐ 中 | Requests |
| 星宇航空 | starlux-airlines.com | ⭐⭐⭐ 高 | Playwright |
| 樂桃航空 | flypeach.com | ⭐⭐⭐ 高 | Playwright |
| 酷航 | flyscoot.com | ⭐⭐⭐ 高 | Playwright |
| AirAsia | airasia.com | ⭐⭐⭐⭐ 極高 | Playwright + 代理IP |
| 捷星亞洲 | jetstar.com | ⭐⭐⭐ 高 | Playwright |

### 4.2 爬蟲微服務架構

```
爬蟲系統
├── Scheduler（排程器）
│   ├── 每 15 分鐘：輕量 HTTP 掃描（檢查頁面 hash 是否變化）
│   ├── 每 1 小時：完整內容爬蟲（Playwright 渲染）
│   └── 即時觸發：偵測社群媒體關鍵字時觸發
│
├── Crawler Workers（爬蟲工作節點）
│   ├── http-scanner.py：輕量 requests 掃描，取 HTML hash
│   ├── playwright-scraper.py：完整 JS 渲染 + 截圖比對
│   └── social-monitor.py：RSS / Facebook Graph API 監控
│
├── Parser Engine（解析引擎）
│   ├── rule-based-parser.py：Regex + CSS Selector 結構化解析
│   └── llm-fallback-parser.py：遇到非結構化文字時呼叫 LLM 解析
│
└── Data Pipeline
    ├── 去重檢查（URL + 日期 + 票價 hash）
    ├── 資料驗證（票價合理性、日期邏輯）
    └── 寫入 Supabase + 觸發推播任務
```

### 4.3 爬蟲核心程式碼片段

```python
# playwright_scraper.py
import asyncio
from playwright.async_api import async_playwright
from supabase import create_client

async def scrape_airline_promo(airline_config: dict):
    async with async_playwright() as p:
        browser = await p.chromium.launch(headless=True)
        context = await browser.new_context(
            user_agent="Mozilla/5.0 (Macintosh; Intel Mac OS X 10_15_7)...",
            viewport={"width": 1280, "height": 800},
            locale="zh-TW"
        )
        page = await context.new_page()

        # 防偵測設定
        await page.add_init_script("""
            Object.defineProperty(navigator, 'webdriver', {get: () => undefined})
        """)

        await page.goto(airline_config['promo_url'], wait_until='networkidle')

        # 等待促銷卡片載入
        await page.wait_for_selector(airline_config['card_selector'], timeout=10000)

        # 取得所有促銷卡片
        cards = await page.query_selector_all(airline_config['card_selector'])
        results = []

        for card in cards:
            data = await extract_promo_data(card, airline_config)
            if data and is_valid_promotion(data):
                results.append(data)

        await browser.close()
        return results

def is_valid_promotion(data: dict) -> bool:
    """基本資料驗證"""
    if not data.get('price_base') or data['price_base'] <= 0:
        return False
    if not data.get('sale_end_at'):
        return False
    if data.get('sale_end_at') < datetime.now():
        return False
    return True
```

### 4.4 LLM 輔助解析（非結構化文字）

當促銷資訊以圖片或不規則文字呈現時，觸發 LLM 解析：

```python
# llm_fallback_parser.py
import anthropic

def parse_promo_with_llm(raw_text: str, screenshot_base64: str = None) -> dict:
    client = anthropic.Anthropic()

    messages = [{
        "role": "user",
        "content": [
            {
                "type": "text",
                "text": f"""請從以下促銷文字中提取結構化資訊，以 JSON 格式回傳：
                {raw_text}

                需要的欄位：
                - 出發地（IATA 代碼）
                - 目的地（IATA 代碼）
                - 促銷未稅票價（數字）
                - 促銷開賣時間（ISO 8601）
                - 促銷截止時間（ISO 8601）
                - 適用旅遊日期範圍
                - 訂票連結（如有）"""
            }
        ]
    }]

    response = client.messages.create(
        model="claude-haiku-4-5-20251001",
        max_tokens=500,
        messages=messages
    )

    return json.loads(response.content[0].text)
```

---

## 五、前端技術架構（Next.js 詳細設計）

### 5.1 頁面渲染策略

| 頁面 | 渲染策略 | 更新頻率 | 原因 |
|---|---|---|---|
| 首頁（促銷看板）| ISR + Realtime | 每 5 分鐘重生 | 高流量 + 需要即時感 |
| 目的地探索頁 | SSG | 每日 | 變動少，SEO 重要 |
| 促銷詳情頁 | ISR | 每 15 分鐘 | 個別促銷變動不頻繁 |
| 航空公司情報頁 | ISR | 每小時 | 半靜態內容 |
| 雷達訂閱 | CSR | 即時 | 個人化，不需 SEO |

### 5.2 關鍵前端組件

```
src/
├── components/
│   ├── PromoCard/
│   │   ├── PromoCard.tsx          # 促銷卡片主體
│   │   ├── CountdownTimer.tsx     # 倒數計時器（客戶端）
│   │   └── ClickCounter.tsx       # 點擊人數顯示
│   ├── FilterBar/
│   │   ├── RegionFilter.tsx       # 地區篩選
│   │   ├── AirlineFilter.tsx      # 航空公司篩選
│   │   └── PriceRangeSlider.tsx   # 票價範圍
│   ├── RadarModal/                # 雷達訂閱彈窗
│   ├── NewDealToast/              # 即時新優惠通知
│   └── TaxEstimator/              # 含稅試算器
├── hooks/
│   ├── useRealtimePromos.ts       # Supabase Realtime hook
│   ├── useCountdown.ts            # 倒數計時邏輯
│   └── useFilteredPromos.ts       # 篩選邏輯
└── lib/
    ├── supabase.ts                # Supabase client
    └── analytics.ts               # 點擊追蹤
```

### 5.3 含稅試算器邏輯

```typescript
// 各廉航常見稅費結構（需定期更新）
const TAX_PRESETS = {
  'IT': { // 虎航
    fuel: 0,
    airport_tax_tpe: 300,
    airport_tax_factor: 0.08,  // 目的地機場稅約 8%
    safety_surcharge: 150
  },
  'MM': { // 樂桃
    fuel: 0,
    airport_tax_tpe: 300,
    airport_tax_factor: 0.10,
    safety_surcharge: 200
  }
  // ... 各航空公司設定
}

export function estimateTotalPrice(
  basePrice: number,
  airlineCode: string,
  isRoundTrip: boolean
): { estimated: number; breakdown: TaxBreakdown } {
  const preset = TAX_PRESETS[airlineCode]
  const multiplier = isRoundTrip ? 2 : 1

  const airportTax = basePrice * preset.airport_tax_factor
  const totalTax = (preset.airport_tax_tpe + airportTax + preset.safety_surcharge) * multiplier

  return {
    estimated: Math.round(basePrice * multiplier + totalTax),
    breakdown: { basePrice, airportTax, fuelSurcharge: preset.fuel, totalTax }
  }
}
```

---

## 六、推播雷達系統設計

### 6.1 LINE Bot 整合

```
LINE Notify 流程：
1. 用戶點「加入雷達」→ 掃 QR Code 加入 LINE Official Account
2. Bot 詢問偏好：目的地 / 預算上限
3. 偏好存入 radar_subscriptions 資料表
4. 爬蟲寫入新促銷 → 觸發 Supabase Edge Function
5. Edge Function 比對訂閱條件 → 發送 LINE Flex Message
```

**LINE Flex Message 範例訊息**：

```json
{
  "type": "bubble",
  "hero": { "type": "image", "url": "大阪城市圖.jpg" },
  "body": {
    "contents": [
      { "text": "🔥 虎航 台北→大阪 限時促銷", "weight": "bold", "size": "lg" },
      { "text": "含稅約 NT$ 2,800", "size": "xxl", "color": "#FF6B35" },
      { "text": "⏰ 距截止 13:45:22", "size": "sm", "color": "#999" }
    ]
  },
  "footer": {
    "contents": [{
      "type": "button",
      "action": { "type": "uri", "label": "→ 立即搶購（官方直達）", "uri": "航空公司官網連結" }
    }]
  }
}
```

### 6.2 Telegram Channel Bot

- 建立 Telegram Channel：`@FlyFromTaiwan`
- 按目的地分 Topic Thread（Telegram Premium Groups）
- Bot 訊息格式：Markdown + 內嵌 Deep Link 按鈕

### 6.3 Email 訂閱（Resend / SendGrid）

- 每日早上 8:00 寄送「今日限時優惠摘要」
- 超低價觸發即時通知（低於用戶設定閾值立即發送）

---

## 七、商業模式

### 7.1 收入來源

| 收入類型 | 說明 | 預估收入 |
|---|---|---|
| **聯盟行銷連結** | 透過 Travelpayouts / 各航空公司官方聯盟計劃，使用者點擊後完成購票，獲得佣金（約票價 1–3%） | 主力收入 |
| **Google AdSense** | 頁面廣告（需控制數量，避免影響使用體驗）| 補充收入 |
| **「雷達 Pro」訂閱制** | 免費版：每週摘要推播；Pro 版（NT$99/月）：即時雷達 + 多目的地 + 價格分析 | 穩定月費 |
| **機票優惠 API 授權** | 資料品質穩定後，可將 API 授權給旅遊部落格、比較網站 | 長期目標 |
| **品牌/航空合作** | 航空公司付費置頂促銷卡、電子報獨家廣告版位 | 成熟期 |

### 7.2 聯盟行銷計劃清單

- **Travelpayouts**：整合多航空公司，統一管理
- **長榮航空官方聯盟**：https://affiliate.evaair.com
- **AirAsia Affiliate**：高佣金（3–5%）
- **Booking.com Partner Program**：飯店交叉銷售

---

## 八、SEO 策略

### 8.1 技術 SEO

- **結構化資料（Schema.org）**：每張促銷卡套用 `Offer` schema
  ```json
  {
    "@type": "Offer",
    "name": "台北飛大阪限時促銷",
    "price": "1499",
    "priceCurrency": "TWD",
    "validFrom": "2025-07-01",
    "validThrough": "2025-07-03"
  }
  ```
- **Open Graph / Twitter Card**：促銷卡片分享到社群自動生成預覽圖

### 8.2 內容 SEO（長尾關鍵字攻略）

- `虎航促銷 2025 九月`
- `台北飛東京 便宜機票 何時買`
- `星宇航空 早鳥票 幾月最便宜`
- `台灣出發 廉航 促銷整理`

每個目的地建立專屬落地頁，定期更新「促銷歷史規律」（何月份最容易有促銷）。

---

## 九、法律與合規

### 9.1 爬蟲合法性

- 僅爬取**公開可見的促銷資訊**，不爬需登入的內容
- 遵守各網站 `robots.txt` 規定
- 設定合理爬蟲間隔（避免對目標伺服器造成負擔）
- 在網站頁腳明確標示「資料來源為各航空公司官方網站」

### 9.2 用戶資料

- 隱私政策頁面：說明推播訂閱資料用途
- GDPR 兼容（若有歐洲用戶）：提供「刪除我的資料」功能
- LINE / Telegram 訂閱資料加密存儲

### 9.3 免責聲明

每個促銷頁面底部加入：
> 「本站資訊僅供參考，票價以各航空公司官方網站為準。促銷活動可能提前結束或已售完，建議確認後盡速至官網完成購買。」

---

## 十、分階段開發藍圖

### Phase 1：MVP 驗證（第 1–4 週）

**目標**：上線可用版本，驗證用戶需求

- [ ] 建立 Next.js 專案 + Supabase 資料庫
- [ ] 手動建立 5–10 條優惠資料（不用爬蟲，先驗證前端）
- [ ] 完成首頁 + 促銷卡片組件
- [ ] 倒數計時器 + 基本篩選功能
- [ ] 部署至 Vercel（上線）
- [ ] 建立 LINE Notify 基本推播

**驗收指標**：網站可正常訪問，手機版顯示正常，可分享單一促銷連結

---

### Phase 2：爬蟲上線（第 5–8 週）

**目標**：自動化資料採集，減少人工作業

- [ ] 建立 3 家航空公司爬蟲（虎航、長榮、樂桃優先）
- [ ] 爬蟲 Cron Job 排程（Render.com 或 Railway 部署）
- [ ] 資料驗證 Pipeline
- [ ] 爬蟲狀態監控 Dashboard（內部用）
- [ ] Supabase Realtime 前端接入（新優惠自動出現）

**驗收指標**：每天自動抓取 ≥ 5 條有效促銷，誤報率 < 10%

---

### Phase 3：黏著度功能（第 9–12 週）

**目標**：提升回訪率與用戶黏著度

- [ ] 「搶票雷達」完整訂閱系統（LINE + Email）
- [ ] 促銷熱度榜（點擊數統計）
- [ ] 含稅試算器完整版（支援 10 家航空）
- [ ] 目的地落地頁 + 促銷歷史圖表
- [ ] Google Analytics 4 + 轉換追蹤

**驗收指標**：LINE 雷達訂閱 ≥ 500 人，每週回訪率 ≥ 30%

---

### Phase 4：商業化（第 13–16 週）

**目標**：建立可持續收入

- [ ] 接入 Travelpayouts 聯盟連結
- [ ] 「雷達 Pro」訂閱付費功能
- [ ] 所有促銷詳情頁 Schema 結構化資料
- [ ] SEO 長尾內容頁 ≥ 20 篇

**驗收指標**：月流量 ≥ 5,000 UV，月聯盟收入 ≥ NT$3,000

---

## 十一、技術堆疊總覽

```
前端 (Front-end)
├── Next.js 15 (App Router)
├── TypeScript (strict mode)
├── Tailwind CSS v4
├── Zustand（全域狀態：篩選條件）
└── Framer Motion（卡片動畫）

後端 / 資料庫 (Back-end)
├── Supabase (PostgreSQL + Auth + Realtime + Edge Functions)
├── Supabase Edge Functions（推播觸發）
└── Upstash Redis（限流、快取熱門資料）

爬蟲 (Crawler)
├── Python 3.12
├── Playwright（JS 渲染）
├── Scrapy（批次爬蟲框架）
├── APScheduler（排程）
└── Railway / Render（爬蟲部署）

推播 (Push Notification)
├── LINE Messaging API
├── Telegram Bot API
└── Resend（Email）

部署 / 基礎設施
├── Vercel（Next.js 前端）
├── Railway（爬蟲微服務）
├── Cloudflare（DNS + DDoS 防護）
└── Sentry（錯誤監控）

分析 / 監控
├── Google Analytics 4
├── Vercel Analytics（Core Web Vitals）
└── Grafana + Loki（爬蟲日誌）
```

---

## 十二、風險評估與對策

| 風險 | 嚴重性 | 對策 |
|---|---|---|
| 航空公司反爬蟲機制 | 高 | Playwright + 隨機 User-Agent + 合理間隔 + 備用代理IP |
| 資料不準確（含稅試算有誤差）| 中 | 明確標示「達人估算，以官網為準」|
| 法律風險（資料版權）| 中 | 只呈現摘要資訊，深層連結至官網；諮詢律師意見 |
| 競爭者複製模式 | 中 | 建立雷達訂閱的用戶基礎（切換成本高） |
| 促銷結束但資料未更新 | 中 | 爬蟲定期重新驗證所有 active 促銷，過期自動標記 |
| 高流量時 Supabase 費用暴增 | 低 | ISR 靜態化 + Upstash Redis 快取，降低 DB 讀取壓力 |

---

*規劃版本：v2.0 | 更新日期：2026-06-09*
